# 进度记录

> 版本：v0.1.0（MVP）｜ 最后更新：2026-07-24

## 当前迭代：MVP v0.1.0 ✅ 核心闭环完成

### ✅ 已完成（功能）
- **Electron 壳**：main / preload / renderer，原生 JS 零构建
- **项目识别**：detector 支持 static / vite / next / nuxt / vue-cli / react-scripts / angular / generic-dev / unknown
- **持久化**：store 存到 `~/Library/Application Support/local-run-frontend/projects.json`，CRUD + 同路径去重
- **一键启动/关闭**：端口扫描顺延、健康检查、进程托管回收
- **内嵌预览服务器**：静态文件 + 路由别名 + 目录页注入（`/__nav__`）+ 路径穿越拦截
- **目录页**：扫描项目 html 自动生成，启动先开目录页（mobile-wshoto 实测 46 个页面卡片）
- **UI**：我的项目 / 收藏 / 最近 / 网关端口管理 / AI Agent 五个视图
- **更新检查**：启动后比对 GitHub release，不一致弹更新提示
- **AI-Agent 控制接口**：`127.0.0.1:47800`，/projects /status /start /stop
- **介绍页**：docs/ 下静态页，版本号强制刷新 + 推广三栏二维码 + 捞鱼署名

### ✅ 已完成（验证）
- 核心单元测试 27 个全过（detector / preview-server / store）
- 所有 JS 语法检查通过
- Electron headless 启动验证：主进程 + 控制 API 正常
- 端到端实测：真实静态项目启动→首页200→目录页46卡→停止→端口释放
- CSP 修复（移除所有 inline handler）
- 落地页 DOM 实测：h1/版本徽章/下载按钮/6功能/3二维码、reward/sticker/mascot 图真实加载

### ⏳ MVP 后阶段（v0.2+）
- `.app` 打包 + 一键安装脚本（去 quarantine）
- Windows `.exe`
- 介绍页部署到 GitHub Pages
- mobile-static 的 routeAliases 规则库（自动映射 `/m/*`）
- token 搬运（qv-admin 那套业务特性）

### ⚠️ 待验证（环境限制）
- **视觉自检**：截图已生成（landing / app UI），但当前环境无法上传到任何图床，
  视觉模型需要远程 URL，故未能用视觉模型复查美观度。已用 DOM 检查替代验证了结构正确性、
  图像真实加载、无占位符残留。**下次在有网络上传能力的环境请用视觉模型复查一下 UI 美观度。**

## 里程碑

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| M1 环境就绪 | ✅ | Electron 43 via npmmirror |
| M2 能启动一个静态项目 | ✅ | 实测 mobile-wshoto 46 页 |
| M3 项目管理 UI | ✅ | 我的项目/收藏/最近/端口/Agent |
| M4 端口管理 + 更新检查 | ✅ | |
| M5 AI-agent 接入 | ✅ | 控制接口 + Prompt |
| M6 打包发布 | ⏳ | .app + 安装脚本（v0.2）|

## 已知问题 / 阻塞

- userData 目录名是 `local-run-frontend`（取自 package.json name），不是中文。
  这不是问题，记录在此避免下次困惑。
- `findFreePort` 偶发：8091 若被占会顺延到 8092（实测正确）。
